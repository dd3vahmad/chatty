---
// import { getUser } from "@/lib/workos";
import { getUser } from "@/lib/api";
// import Layout from "@/layouts/AppLayout.astro";

const user = await getUser();
if (!user) {
  console.log("No user - Error fetching user");
  return;
}
console.log(user);
// const user = await getUser(Astro.cookies);

// const avatarUrl = user?.profilePictureUrl;

// const joinDate = user?.createdAt 
//   ? new Date(user.createdAt).toLocaleDateString('en-US', { 
//       year: 'numeric', 
//       month: 'long', 
//       day: 'numeric' 
//     })
//   : "Unknown";
---

<!-- 
<Layout title={`${user?.firstName || 'User'}'s Profile`}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-md overflow-hidden">
      <div class="relative h-48 bg-gradient-to-r from-blue-500 to-purple-600">
        <div class="absolute -bottom-16 left-6">
          <div class="rounded-full border-4 border-white dark:border-gray-800 shadow-lg overflow-hidden h-32 w-32">
            <img src={avatarUrl} alt="Profile Picture" class="h-full w-full object-cover" />
          </div>
        </div>
      </div>

      <div class="pt-20 px-6 pb-6">
        <div class="flex justify-between items-start">
          <div>
            <h1 class="text-2xl font-bold text-gray-900 dark:text-white">
              {user?.firstName || ''} {user?.lastName || ''}
            </h1>
            <p class="text-gray-600 dark:text-gray-300">@{user?.username || user?.email?.split('@')[0] || 'username'}</p>
          </div>
          
          <button class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-medium transition-colors">
            Edit Profile
          </button>
        </div>

        <div class="mt-6">
          <p class="text-gray-700 dark:text-gray-300">
            {user?.bio || "Hey there! I'm using ChatApp to connect with friends and family."}
          </p>
        </div>

        <div class="mt-8">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Additional Information</h2>
          <div class="space-y-3">
            <div class="flex items-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-500 dark:text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              <span class="text-gray-700 dark:text-gray-300">{user?.email || "user@example.com"}</span>
            </div>
            <div class="flex items-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-500 dark:text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
              <span class="text-gray-700 dark:text-gray-300">Joined {joinDate}</span>
            </div>
            <div class="flex items-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-500 dark:text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
              <span class="text-gray-700 dark:text-gray-300">{user?.location || "San Francisco, CA"}</span>
            </div>
          </div>
        </div>

        <div class="mt-8">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Recent Activity</h2>
          <div class="space-y-4">
            {Array(3).fill().map((_, i) => (
              <div class="flex items-start space-x-3 p-3 bg-gray-50 dark:bg-gray-700 rounded-lg">
                <div class="h-10 w-10 rounded-full overflow-hidden flex-shrink-0">
                  <img src="/api/placeholder/40/40" alt="Activity" class="h-full w-full object-cover" />
                </div>
                <div>
                  <p class="text-gray-800 dark:text-gray-200">
                    {["Sent a new message", "Created a new group", "Added a new connection"][i]}
                  </p>
                  <p class="text-sm text-gray-500 dark:text-gray-400">
                    {["2 hours ago", "Yesterday", "3 days ago"][i]}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout> -->